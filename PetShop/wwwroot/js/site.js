// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.
//$(document).ready(function () {
//    $("#catalog-categories").change(function () {
//        var selectedValue = $(this).val();
//        fetch("/Home/Catalog/" + selectedValue)
//            .then(res => window.location.href = "/Home/Catalog/" + selectedValue)
//            .then($("#categories").val(selectedValue))
//            .catch(error => console.log(error));
//    });


    //$('.comment-form').submit(function (event) {
    //    let content = $('#content').val().trim();

    //    if (!content) {
    //        alert("Please enter comment content.");
    //        event.preventDefault();
    //        return false;
    //    }

    //    if (content.length < 1 || content.length > 300) {
    //        alert("Comment content must be between 1 and 300 characters.");
    //        event.preventDefault();
    //        return false;
    //    }
    //});
});

